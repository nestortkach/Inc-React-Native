import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primaryText: '#F5F5F5',
    background: '#0F0F0F'
  }
};

export default theme;
